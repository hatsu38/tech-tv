<% content_for :title, "#{@tag.name}タグの勉強会一覧 | こんぱすちゅ〜ぶ" %>
<%
  content_for :meta_description,
  "こんぱすちゅ〜ぶ（#{@tag.name}）一覧です。こんぱすちゅ〜ぶでは、勉強会のイベントを紹介しています。"
%>
<% content_for :meta_keywords, "#{@tag.name}" %>
<% content_for :og_title, "こんぱすちゅ〜ぶ（#{@tag.name}タグの勉強会一覧" %>
<%
  content_for :og_description,
  "こんぱすちゅ〜ぶ（#{@tag.name}）タグの勉強会一覧です。こんぱすちゅ〜ぶでは、勉強会のイベントを紹介しています。"
%>
<%= react_component('TagDetail', {tag: @tag, events: @events, totalCount: @total_count}) %>
<%= react_component('NewlyEvents') %>
<%= react_component('PopularTagRelatedEvents') %>